{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "atb db verify --apply-migrations && atb live-health ml_basic",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
