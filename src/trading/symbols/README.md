# Trading Symbols

Symbol normalization helpers for translating between exchange-specific naming
schemes (e.g., Binance `BTCUSDT` vs Coinbase `BTC-USD`). Add new exchanges here
so every subsystem can reuse the same mapping logic.
