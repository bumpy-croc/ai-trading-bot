# Ops Snapshot — 2026-06-05 08:08 UTC
**Overall**: yellow
**Environment**: not running (no live or paper process active)

---

## Process

- `atb live` is NOT running. No trading process found (paper or live).
- The only "atb"-matching process in pgrep is the Claude Code agent session itself.
- Health endpoint: no service listening on port 8000. Calling `atb live-health` launched and immediately exited with an arg-parse error (unrecognized argument `--paper-trading`) — this indicates the health subcommand syntax has drifted from docs.
- Operating mode per charter: **UNFILLED (TODO)**. Charter has not been completed by the human board — all `TODO` placeholders remain. Operating mode, capital, environments, escalation method, and KPI targets are all unset.

## Database

- `DATABASE_URL` not set in environment.
- PostgreSQL is NOT running. Docker is available but no postgres container is active (`docker ps` shows nothing). `pg_isready` on localhost:5432 returned "no response".
- `atb db verify` failed at connection stage: `(psycopg2.OperationalError) connection to server at localhost:5432 failed: Connection refused`.
- No migrations, table checks, or trade data readable in this state.

## Trading Activity (last 24h)

- No data accessible — database offline.
- No `logs/` directory exists in the project root.
- No prior ops snapshots exist (first run).

## Open Positions

- Cannot verify — database unreachable.
- No in-memory state available (no running process).

## Data Freshness

- Cannot check — no data provider process running, no DB.

## Error Log

- No application log files under `logs/` (directory does not exist).
- `/tmp/claude-code.log` contains only Claude Code agent harness debug entries — no application errors.

## Railway

- `railway` CLI is not installed in this environment.
- No Railway environment variables present (`RAILWAY_*` not in env).
- Cannot verify staging or production deployment status, restart count, CPU/memory.

## Incidents

- No incident files in `.claude/state/incidents/` (only README.md present).
- GitHub `gh` CLI not available in this remote environment (using GitHub MCP instead — not queried here due to read-only scope of this check).

## Charter / Configuration State

- Charter: **not configured**. All mission, operating mode, capital, escalation fields are `TODO`. This means the daemon cannot make material decisions per its own rules.
- `risk-limits.json`: present and structurally valid (reviewed). Hard limits defined: max drawdown 20%, max daily risk 6%, max position 10%, max consecutive errors 10. Last reviewed field is `1970-01-01` — never formally reviewed.
- `wakeups.jsonl`: empty (no scheduled wakeups).
- `log.md`: initialized but empty (only the system init entry).

## Anomalies

- [P1] No trading process running (neither paper nor live). If a process was expected to be up, this is degraded.
- [P1] PostgreSQL not running. All DB-dependent checks are blocked.
- [P2] `railway` CLI absent — Railway deployment status for staging/production cannot be verified from this environment.
- [P2] `atb live-health` argument parsing error: `--paper-trading` unrecognized by the `runner` subcommand. Docs/CLI interface mismatch.
- [P3] Charter completely unfilled. Operating mode unknown — cannot classify "paper vs live" authoritatively without human completing charter.md.
- [P3] `risk-limits.json` `$last_reviewed` is epoch zero — field was never updated after initial setup.
- [P3] No `logs/` directory. Application logging may be going to stdout only (acceptable in Railway/container deployments, but makes local forensics impossible).

## Actions Taken

- 08:08 UTC: Read charter, risk-limits, state log, incidents directory.
- 08:08 UTC: Attempted `atb db verify` — failed (no DB_URL, then no DB connection).
- 08:08 UTC: Checked for running processes — none found.
- 08:08 UTC: Checked port 8000 — not listening.
- 08:08 UTC: Checked Railway CLI — not installed.
- 08:08 UTC: No process restarts attempted (not authorized without clearer operating mode context).

## Escalations

- None paged. No P0 conditions (live capital not at risk because no live process is running and no open positions are known).
- Human attention recommended to: (1) complete charter.md, (2) start Postgres if local testing intended, (3) confirm whether a trading process should be running in Railway staging/prod.
