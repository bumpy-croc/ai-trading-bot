# Ops Snapshot — 2026-08-13 15:03 UTC

**Overall**: yellow
**Environment**: both (prod=live, staging=paper)
**Context**: read-only sweep after ~3.5 weeks with no detailed live-ops check (last: 2026-07-21). Railway CLI is UNAUTHORIZED this session (`railway status`/`railway logs` both fail — recurring known issue, see MEMORY `project_railway_cli_staging_auth_recurring`). All findings below are DB-only; no application-log sweep (LESSONS §5.6 item) was possible, and no confirmation of current Railway deploy id / FEATURE_* flag values was possible. Assume nothing has been watched — this sweep is the first ground-truth check since 2026-07-21.

## PROD (live capital, session 20, ETHUSDT/HyperGrowth)

### Process / liveness
- `account_history` heartbeat: **clean, ~60min cadence, zero gaps > 2h over the full 7-day window checked** (166 rows, 2026-08-06→08-13). Latest row 2026-08-13 14:47:44 UTC (~15 min old at sweep time).
- Position row (`positions.id=25`) `last_update` = 2026-08-13 15:03:30 UTC, essentially real-time — confirms the trading loop is actively ticking right now, not just the hourly snapshot job.
- Active session: id=20, started 2026-06-05 20:59:45, `is_active=true`. No newer session — same session has run continuously ~10 weeks.

### Money
- Balance (cash): $83.64481379 (flat since 2026-07-21, matches "only 1 trade closed in the window" below).
- Equity (mark-to-market): $83.43850407 at 14:47:44 UTC, oscillating $83.16–$83.71 over the last 7 days.
- Peak equity since session start (2026-06-05): $84.44440224. Current drawdown from peak: **1.19%** — well inside the 15%/20% guard bands.
- Open position: id=25, LONG ETHUSDT, entry $1918.51 @ 2026-07-21 17:55:02, qty 0.0087 ETH, stop_loss $1726.524, take_profit $2493.868, unrealized_pnl **-$0.216 (-0.24%)** at last check (current_price $1894.72). Position has been open **23 days**. Entry order (id 7430) FILLED; no separate SL row exists in `orders` (expected per LESSONS §5.4 — the resting SL is tracked on the position record, not re-inserted into `orders` each sync cycle).
- Trades closed since 2026-07-21: **1** (id=15, LONG, +$0.409, exit_reason=stop_loss, closed 2026-07-21 17:48:43 — the trade that preceded position #25's entry). Net P&L over the window: **+$0.41**.
- Fees over trailing 30d: $0.038 across 2 trades.
- No orders in CANCELLED/REJECTED state in the last 30 days (3 FILLED total).

### Risk state
- `system_control_flags` table: **empty** — no SYSTEM_HALT, no manually-set flags of any kind in the DB.
- FEATURE_ENTRY_PAUSE / FEATURE_ACCOUNT_CIRCUIT_BREAKERS current values: **could not verify** — requires `railway variables`, CLI unauthorized. Last known state (2026-07-17 log entry): entry-pause was found stuck-on for ~95h due to a scheduled-task fragility (GH #1038, since resumed); circuit breakers OFF in prod, `dry_run` in staging as of 2026-07-14.
- Circuit-breaker prod arming (GH #986): **still OPEN, HOLD verdict never re-visited.** The pre-committed 14-day staging dry_run clean-window (restarted 2026-07-14 09:46 UTC) completed **2026-07-28 09:46 UTC — 16 days ago** with no log.md entry evaluating pass/fail and no comment on #986 since 2026-07-14. `system_events` shows **zero** `CIRCUIT_BREAKER_DRY_RUN` rows in the whole window (expected/known — #986's own evidence comment documents dry-run trips log only to ephemeral stdout, never to `system_events`, so this DB can't independently confirm a clean window). This is a **stale decision-pipeline item**, not a live-capital risk today (drawdown is 1.2%, nowhere near either threshold) — flagging as P2/process gap, not incident.
- MaxDrawdownGuard peak: last durably confirmed 2026-07-12 at $84.4159 (session-scoped, DB-seeded); current peak $84.4444 is consistent with organic drift, no evidence of a reset artifact.

### Events (system_events since 2026-07-21)
Only **one** row in the entire 23-day window:
- `2026-08-08 22:33:26 UTC` — **CRITICAL ALERT**: "User data stream circuit-open after 3 reconnects — REST-degraded; real-time fills/balance updates unavailable (subscription age 2.0m)".
- **No recovery event since.** The last "User data WebSocket recovered" row in the table is from 2026-07-20 00:54 — i.e. the WS has apparently been sitting in REST-degraded mode for **5 days and counting** with nothing logging its return to WS-primary. This matches a known issue class (MEMORY `project_binance_margin_ws_churn`: "user stream degrades to REST until restart — #723/#724 open"). The loop itself is clearly alive (position last_update is current, hourly heartbeat unbroken, price feed updating), so this is a **degraded, not down**, condition — REST polling appears to be covering price/position updates fine. But per the known pattern this typically only clears on a process restart, and live-ops is **never authorized to restart a live-capital process**. **Flagging as an anomaly for escalation** (see below) — nobody has looked at or acknowledged this in the 5 days since it fired.
- Zero HIGH/CRITICAL reconciliation_audit_events since 2026-08-08 (none at all since 2026-07-20).
- No SHORT_ENTRY_SUPPRESSED events in the window (expected — long-only mode per #1030, no short signals to suppress).

### Deploy
- Local `origin/main` HEAD is still `bf7f45cb` (same commit referenced in the 2026-07-13 #986 evidence pull as "currently deployed"). Nothing has merged to `main` since. Prod DB `alembic_version` = `0013_widen_event_type`, which matches the latest migration file in the repo tree (`0013_widen_system_events_event_type.py`) — **no pending migrations, schema in sync.**
- Could not independently confirm the live Railway deployment id (CLI unauthorized) — inferring "still on bf7f45cb" from git + DB-schema consistency only, not from Railway's own deploy record.

### Log-signature sweep (LESSONS §5)
**Not performed** — no application log access this session (Railway CLI unauthorized). The only signal available is the single system_events row above (WS circuit-open). No stop-loss failures / -2010 / -1111 / 51077 / emergency-close / "No active trading session" rows appear in `system_events` or `reconciliation_audit_events` for the window, but these signatures normally show up in stdout logs first — their absence from the DB is not strong evidence of absence in logs.

## STAGING (paper, session 23, ETHUSDT/HyperGrowth)

### Process / liveness
- `account_history` heartbeat: clean, ~60min cadence, max gap 1h01m over the last 7 days (166 rows). Latest row 2026-08-13 14:37:44 UTC.
- Active session: id=23, started 2026-07-14 09:46:15 (deploy e7c74349 / PR #1035), `is_active=true`.

### Money
- Balance: $1021.90639593 (flat since the last close on 2026-07-27).
- Equity: $1016.62086164 at last snapshot, oscillating.
- Open position: id=44, LONG ETHUSDT, entry $1946.64 @ 2026-07-27 12:33:08, size 0.199, stop_loss $1751.10, take_profit $2529.37, unrealized_pnl **-$5.30 (open 17 days)**.
- Trades closed since 2026-07-21: **1** (id=39, +$2.85, exit_reason=Stop loss, closed 2026-07-27 12:33:01 — the trade that preceded position #44). Net P&L over window: **+$2.85**.
- `strategy_executions.action_taken='opened_long'` shows 150 rows in the last 3 days — this is the **known logging artifact** (each ~30–60s loop iteration re-logs the still-open position's state as "opened_long"; documented in log.md 2026-07-12 finding, overstates real entries ~300x). Not a sign of 150 real new positions — only 1 position and 1 trade exist in the window.

### Deploy
- Repo HEAD for the deployed commit (per PR #1035 / log.md) is `2f6c1fe8` (develop tip merged via `3cd4ce31` on 2026-07-14); nothing has been re-deployed to staging since per the log/PR trail. Could not independently verify current commit via Railway CLI (unauthorized).

### Circuit-breaker dry-run clock (GH #986 / [D-2026-07-14-03])
- Window restarted 2026-07-14 09:46 UTC, was due to complete 2026-07-28 09:46 UTC (14 clean days) — **now 16 days past due, never evaluated.**
- `CIRCUIT_BREAKER_DRY_RUN` events since 2026-07-14: **0** (matches the known instrumentation gap documented in #986 — dry_run trips only log to ephemeral container stdout, not `system_events`; the DB cannot independently confirm a clean window either way).
- `FEATURE_ACCOUNT_CIRCUIT_BREAKERS` current value: could not verify (CLI unauthorized); last confirmed `dry_run` as of 2026-07-14 deploy note.
- Given the DB can't distinguish "quiet market, breaker never got exercised" from "breaker silently broken," and the pre-committed criteria explicitly required the window to "span >=1 restart and >=1 realistic open-position hold" — staging has had a position open continuously since 07-27 (17 days) and the deploy itself represents 1 restart, so the *conditions* for a meaningful window were arguably met, but nobody has produced the pass/fail verdict.

### #1045 (zero-size decisions) — status: not currently reproducing
- No `entry` rows with `position_size = 0` in the last 10 days. Current state is simply "in-position, entries correctly skipped" — not the silent-zero-size bug. Cannot rule out recurrence when the position eventually closes and the strategy re-evaluates entries; the underlying root cause (GH #1045) is still open/unfixed.

### #1044 (phantom OPEN rows) — status: unchanged, still present
- `positions` table still shows **20 total OPEN rows** across 11 distinct session ids (1 real: session 23/id 44; 19 stale, spanning sessions 1–14 from April–July). Matches the issue's "~19-20" figure exactly — no worsening, no remediation yet. Not a capital risk (staging is paper, and the engine's own in-memory/`account_history.open_positions` view is unaffected), but any analytics or forensics query against `positions.status='OPEN'` without an explicit session filter will still be wrong.

### Staging system_events since 2026-07-21
- **Zero rows** of severity warning/critical/error. No CIRCUIT_BREAKER_DRY_RUN, no SHORT_ENTRY_SUPPRESSED (consistent with continuous in-position state, no short signals to suppress).

## Anomalies (ranked)

1. **[P1 — escalate, do not restart]** Prod: user-data WebSocket has been REST-degraded since 2026-08-08 22:33 UTC (5 days), critical ALERT fired once, never recovered per the DB, never followed up by anyone (PM has been away since 07-21). Matches known #723/#724 pattern (WS degradation typically requires a process restart to clear) — but live-process restarts are outside live-ops authorization. Recommend PM/human decide on a restart during a low-risk window, and verify via Railway logs (once CLI auth is restored) whether the degradation is ongoing or already silently self-healed without an event being logged.
2. **[P2 — process gap]** GH #986 circuit-breaker dry-run arming decision: pre-committed 14-day staging window completed 2026-07-28, 16 days ago, no verdict recorded. Not a capital risk today (drawdown nowhere near threshold on either environment) but is exactly the kind of "nobody's watching" gap the task called out — should be picked up in the next `/triage`.
3. **[P2 — tooling]** Railway CLI unauthorized on this machine/session — blocks deploy-id confirmation, FEATURE_* flag verification, and the entire application-log signature sweep (LESSONS §5, task item 6/8 partial). Matches the recurring auth-drop pattern already logged in MEMORY; needs a human re-login, not another automated retry.
4. **[P3 — known, unchanged]** Staging phantom OPEN rows (#1044) still ~20, unfixed. No new occurrences since last check, not growing.
5. **[P3 — known, unchanged]** Both prod (#25, 23d) and staging (#44, 17d) are sitting in long-held single positions — expected for this low-turnover strategy, but it does mean most of the recent monitoring window has near-zero trading activity to observe. Not itself an anomaly, just context for why "trades since 07-21" is n=1 on both sides.

## Actions taken
- None (read-only sweep only, per task instructions). No restarts, no flag changes, no `railway` mutating commands attempted.

## Escalations
- **Recommended, not yet actioned by this sweep** (task explicitly said no chips / no sub-delegation): the human (Alex) should be made aware of (1) the prod WS REST-degraded state open 5 days with no follow-up, and (2) the overdue #986 circuit-breaker verdict. Both should be raised in the next standup/triage cycle; neither is P0 (no capital-at-risk signal, no divergence between DB and reality, no duplicate orders, drawdown 1.2%).
