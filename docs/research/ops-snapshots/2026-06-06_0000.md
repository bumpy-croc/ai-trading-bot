# Ops Snapshot — 2026-06-06 00:00 UTC

**Overall**: yellow
**Environment**: live (production on Railway inferred from log.md; charter TODOs unfilled)

---

## Process

- **Local**: No `atb live` or Python trading processes running on this host (`pgrep` returned empty).
- **Health endpoint**: Not reachable locally — `curl localhost:8000/health` and `:8080/health` both returned connection-refused (exit 7). Expected: bot runs on Railway, not locally.
- **Railway CLI**: Not installed on this host. Cannot directly query Railway service status, restarts, or resource metrics.
- **Docker**: Docker daemon not running on this host; cannot check containers locally.
- **Environment vars**: No `DATABASE_URL`, `BINANCE_API_KEY`, or `TRADING_MODE` set in local shell. `atb db verify` failed with "DATABASE_URL is required but not set."

---

## Database

- **Local DB**: Unreachable — no `DATABASE_URL` in environment.
- **Railway DB**: Cannot query directly from this host (no credentials set locally).
- **Last known DB state** (from log.md, 2026-06-05 incident-resolve entry):
  - Prod DB had 2 OPEN ETHUSDT rows: #12 (phantom, 0.0033 ETH, session 17) and #13 (real, 0.0037 ETH, session 18).
  - Phantom #12 was closed with human approval. Only position #13 remains: 0.0037 ETH, SL order `47181104013`.
  - Account equity at time of reconciliation: **$83.28** (exchange truth) vs $84.13 tracked (~1% overstatement).
  - The $0.85 overstatement is booked to true equity on next FLAT event via `_sync_margin_equity`.
- **Migration status**: Unknown (cannot run `atb db verify`).

---

## Trading Activity (last known state)

- **Open positions**: 1 × ETHUSDT (position #13, session 18, ~0.0037 ETH, SL active: `47181104013`).
- **Recent trades**: Cannot query `trades` table — no DB connection from this host.
- **Strategy**: `ml_basic` listed as Production; `ml_adaptive`, `ml_sentiment`, `ensemble_weighted` also Production per project_status.md.
- **Active symbols (inferred)**: BTCUSDT, ETHUSDT (ML models present for both).

---

## Git / Code State

- **Current branch**: `claude/gifted-cannon-wsmrh` (feature branch, 14 commits ahead of `origin/develop`).
- **Most recent commits on current branch** (all are fixes merged back from production-track develop):
  - `#699` — quantize stop-loss to tick precision (avoid -1111 error)
  - `#695` — quantize order qty to step-size (avoid 51077 error)
  - `#693` — register reused session with db_manager on active recovery (P0 #41)
  - `#691` — `_send_alert` HTTP status fix
  - `#688` — structured error/event emission + alerting
  - `#687` — Railway healthcheckTimeout raised 10s → 300s
- **origin/develop** is 5 commits behind HEAD (i.e., develop has not yet received the `#699`/`#695`/`#693` fixes).

---

## ML Models

| Symbol | Type | Latest version |
|--------|------|---------------|
| BTCUSDT | basic | `latest` symlink present |
| BTCUSDT | sentiment | `latest` symlink present |
| ETHUSDT | sentiment | `latest` symlink present (2025-12-23_21h_v1) |

- ETHUSDT sentiment model evaluation: MAPE = 99.7% (essentially random; this model should not be trusted for live signals without retraining).
- No `ETHUSDT/basic/` model directory found — only sentiment type for ETHUSDT.

---

## Recent Incident History

- **2026-06-05 phantom-position incident** (resolved): Prod had phantom position #12 alongside real #13. Human-approved DB write closed phantom. Revert of #679 (`adopt-all`) merged. PR #682 (#677-only safe recover) in prod. Filed #683 for redesign.
- No incident files with `status: open` found in `.claude/state/incidents/`.
- GitHub Issues: `gh issue list` unavailable (no GitHub remote configured for `gh` CLI in this environment).
- No `type:incident` GitHub issue state accessible from this host.

---

## Anomalies

| # | Severity | Finding |
|---|----------|---------|
| 1 | P2 | Railway CLI not installed on this host — cannot verify Railway deployment status, restarts, memory/CPU, or environment health directly. All Railway checks require `railway` CLI or MCP tools. |
| 2 | P2 | `origin/develop` branch is 5 commits behind current branch; the most recent production-hardening fixes (#699, #695, #693) have NOT been merged back to develop yet. Risk of develop diverging further from prod. |
| 3 | P2 | `DATABASE_URL` not set locally — live DB state unverifiable from this host. All DB checks are blocked. |
| 4 | P3 | Charter `charter.md` has all material fields as TODO: operating mode, capital, environments, active symbols, escalation method. The daemon cannot make material decisions until these are filled by the human. |
| 5 | P3 | `risk-limits.json` `$last_reviewed` is `1970-01-01` — never formally reviewed by a named human. |
| 6 | P3 | ETHUSDT sentiment model MAPE = 99.7% — essentially useless. If `ml_with_sentiment` or `ensemble_weighted` uses this model for ETHUSDT signals, strategy quality is severely degraded. |
| 7 | P3 | No `ETHUSDT/basic/` model found; only sentiment. If `ml_basic` is being run on ETHUSDT, it may fall back to an unexpected code path. |
| 8 | P3 | $0.85 (~1%) equity overstatement on prod account pending write-back at next FLAT event. Not yet confirmed resolved. |

---

## Actions Taken

- None. Read-only snapshot. No processes were modified.
- `docs/research/ops-snapshots/` directory created (was missing).

---

## Escalations

- None at this time (no P0/P1 conditions confirmed from available data).
- **Recommend**: Human to fill `charter.md` TODOs (operating mode, environments, escalation path) so the daemon can operate with full authority and page correctly on future incidents.
- **Recommend**: Merge current branch fixes (#699/#695/#693) back to `develop` to prevent drift.
- **Recommend**: Check Railway dashboard manually for: deployment status across development/staging/production, recent restarts, resource utilization, and current ETHUSDT position #13 status since the 2026-06-05 reconciliation.
