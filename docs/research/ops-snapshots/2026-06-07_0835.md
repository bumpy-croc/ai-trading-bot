---
snapshot_at: 2026-06-07T08:35:00Z
severity: yellow
environment: paper (development + staging + production assumed)
operator: live-ops
---

## Ops Snapshot — 2026-06-07 08:35 UTC

**Overall**: yellow
**Environment**: paper / Railway (development, staging, production — project: innovative-transformation)

---

### 1. Charter state

Charter is in TEMPLATE state — all TODO markers unfilled (mission, trading mode, capital, symbols, risk limits, escalation method, KPIs). The daemon is operating read-only; no material decisions possible without a completed charter.

---

### 2. Process — local container

No `atb live` process is running locally. This is expected: the bot runs on Railway, not in the dev container. No local health endpoint to probe.

---

### 3. Railway MCP / CLI

- Railway CLI: not installed in this container.
- `RAILWAY_API_TOKEN`: not set in environment. Direct Railway API calls returned 401 (Not Authorized).
- Railway MCP server tools (`mcp__Railway__*`) are not available in this session.
- **No direct Railway deployment/log access is possible from this container.**

---

### 4. Heartbeat monitor (primary liveness signal)

Source: GitHub Actions `heartbeat-monitor.yml` — reads `account_history` from both staging and production DBs every 30 min.

| Run | Timestamp (UTC) | Result |
|-----|----------------|--------|
| #103 | 2026-06-07 07:35 | SUCCESS |
| #102 | 2026-06-07 05:40 | SUCCESS |
| #101 | 2026-06-07 03:49 | SUCCESS |
| #100 | 2026-06-07 02:24 | SUCCESS |
| #99  | 2026-06-07 01:20 | SUCCESS |
| (all 30 runs back to ~2026-06-06 ~21:00) | | ALL SUCCESS |

**Conclusion**: Both staging and production bots are writing `account_history` snapshots within the 120-minute staleness threshold. Liveness confirmed as of 07:35 UTC today (~55 min ago from snapshot time).

---

### 5. Database connectivity

- `DATABASE_URL` is not set in the local container environment.
- Local PostgreSQL is unreachable (connection refused on localhost:5432; postgres hostname unresolvable).
- Cannot run `atb db verify` or direct SQL queries from this context.
- DB health is inferred indirectly from heartbeat monitor success (the script connects read-only to both staging and production DBs).

**Inference**: Both staging and production databases were connectable and returning rows at 07:35 UTC.

---

### 6. Recent CI / workflow activity (2026-06-07)

| Time (UTC) | Workflow | Result | Notes |
|------------|----------|--------|-------|
| 07:35 | Heartbeat Monitor | SUCCESS | |
| 05:40 | Heartbeat Monitor | SUCCESS | |
| 04:59 | Optimizer MVP | FAILURE | Step: "Run optimizer (fixture provider)" — not a trading health issue |
| 04:32 | Performance Tests | SUCCESS | |
| 03:49 | Heartbeat Monitor | SUCCESS | |
| 02:59 | Weekly Model Training | FAILURE | Step: "Verify required secrets are present" — AWS/SageMaker secrets not configured; affects BTCUSDT and ETHUSDT training jobs; not a live trading issue |
| 02:24 | Heartbeat Monitor | SUCCESS | |

---

### 7. Known open state (from log.md)

Last material log entry (2026-06-05): Phantom position #12 resolved in production. Current prod state:
- Active position: #13 (ETHUSDT, 0.0037 ETH, session 18, SL `47181104013`).
- Account equity: ~$83.28 (DB tracked $84.13 at time of reconciliation; ~$0.85 overstatement to be corrected at next FLAT moment by `_sync_margin_equity`).
- Phantom #12 was closed in DB; no double-exposure.
- PR #683 filed to redesign adopt-all (exchange-verified-before-adopt); #679 reverted on develop.

Recent commits (HEAD = `5548373`): base-asset exchange-mutation lock (#703/#705) — enables safe sweep ACTIVE mode. Deployed to production via promote branch.

---

### 8. Anomalies

| Severity | Description |
|----------|-------------|
| P3 | Charter is fully in template state — no mission, mode, escalation, or KPIs defined. Material decisions blocked. |
| P3 | `RAILWAY_API_TOKEN` not set in this container — cannot inspect Railway deployment logs, CPU/memory, or restart count directly. |
| P2 | Weekly Model Training failing every week — AWS/SageMaker secrets (`AWS_ACCESS_KEY`, `AWS_SECRET_KEY`, `SAGEMAKER_ROLE_ARN`, `SAGEMAKER_S3_BUCKET`) not configured as repo secrets. Models are not being retrained automatically. |
| P2 | Optimizer MVP workflow failing — "Run optimizer (fixture provider)" step. Non-critical to live trading but warrants investigation. |
| P3 | Production DB shows ~$0.85 balance overstatement pending correction at next FLAT moment (known, tracked, race-free resolution in progress). |

---

### 9. Actions taken

None. Read-only snapshot. No Railway access. No local DB access.

---

### 10. Escalations

None required at this time. Both bots are alive (heartbeat clean). No P0/P1 conditions detected via available signals.

---

### Limitations of this snapshot

This snapshot could not access:
- Railway deployment status, restart count, memory/CPU directly (no token/MCP).
- Live log lines from the running engines.
- Database tables (trades, positions, sessions, performance_metrics) directly.
- Active strategy name, open position count, account balance from DB.

The heartbeat monitor is the sole direct liveness signal available and it is green.
