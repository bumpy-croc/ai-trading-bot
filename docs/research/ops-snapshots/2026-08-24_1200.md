## Ops Snapshot — 2026-08-24 12:00 UTC
**Overall**: red (detection-failure P1; capital was never at risk)
**Environment**: live (production)

### Process
- Trading Bot service (Railway, production): deploy `fba64281` (PR #1066, promoted 2026-08-13), status SUCCESS, **no restart since deploy**. Same deploy was running throughout the incident window.
- **Loop is ALIVE, not dead.** `Decision:` lines flowing at normal ~60–140s cadence as of 2026-08-24 11:53 UTC. `account_history` heartbeat current (writer is not gated by close-only).
- **Close-only mode latched** 2026-08-20 16:48:51 UTC, still active. Purely in-process (`trading_engine.py:782` `_close_only_mode` bool) — `system_control_flags` table is empty, so nothing persists this outside the running process. No CLI/API clears it; `resume_trading()` has zero external callers. Only a full service restart clears it (re-runs startup reconciliation).

### Database
- Read-only connection via `DATABASE_PUBLIC_URL` (production Postgres), `SET default_transaction_read_only = on`.
- `max(strategy_executions.timestamp)` = 2026-08-20 16:49:43 (frozen — entry-check function returns before its logging call when close-only is active).
- `max(system_events.timestamp)` = 2026-08-20 16:48:52 (same cause).
- `max(account_history.timestamp)` = 2026-08-24 11:43:42 (current — heartbeat unaffected).
- `orders`/`trades`: 0 ENTRY orders, 0 new trades since 2026-08-20 16:49. 0 open positions now.

### Trading activity (last 24h)
- Trades: 0, rejects: 0 (no entries attempted — blocked by close-only)
- Open positions: none. Balance/equity ≈ $87.27, flat.
- Last 3 closed trades (all profitable "Stop loss" exits, trailing-stop pattern): id 16 (+8.00%), id 17 (+10.46%), id 18 (+3.63%, closed 2026-08-20 16:49:49 — the position whose SL re-placement failure triggered close-only).

### Anomalies
- **[P1]** Close-only mode latched ~4 days, undetected by daily standup. Root cause and full timeline in incident file / GH #1094.
- **[P1]** `daily-trading-standup`'s "engine alive" check (Decision: log cadence) structurally cannot detect this failure mode — GH #1095.
- **[P3]** `BinanceProvider.place_stop_loss_order` swallows the real exchange error text into a bare `logger.error`, never reaching `system_events`/`reconciliation_audit_events` — combined with Railway's no-history log retention, this made the 2026-08-20 SL rejection's root cause unrecoverable 4 days later.
- **[Info, positive]** An operator alert *was* sent via the configured Slack webhook (`alert_sent=true, alert_method=webhook` on both `system_events` rows for the close-only transition) — needs human confirmation on whether it was seen.

### Actions taken
- 2026-08-24 ~11:50–12:10 UTC: read-only forensic sweep (Postgres, Railway status/logs, source code). No writes to prod, no restart, no flag changes.
- Filed incident `.claude/state/incidents/2026-08-24T1200-P1-close-only-4day-silent-halt.md`, PR #1093 (branch `docs/incident-close-only-halt-0824`, targets `develop`).
- Filed GH #1094 (incident) and GH #1095 (standup detection-gap fix), cross-linked.

### Escalations
- Human (per role authorization matrix — restart/clear of a live-capital process requires escalation, never authorized for this agent). PM/human should: (1) confirm the Slack alert was actually seen, (2) decide on restart timing/authorization to clear close-only, (3) prioritize GH #1095 so this failure mode is detectable going forward.
