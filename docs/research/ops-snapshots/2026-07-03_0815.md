# Ops Snapshot — 2026-07-03 08:15 UTC

**Overall**: yellow
**Environment**: both (Railway `production` / `main`, and `staging`)

---

## Access constraints (read first)

This session has **no Railway CLI**, **no `RAILWAY_*_DATABASE_URL` / `DATABASE_URL`**, and **no local Postgres/Docker**
available — consistent with the prior snapshot (2026-06-04). `atb db verify`, direct `trades`/`positions`/
`account_history` queries, and `railway logs`/`railway status` are all **unavailable** in this sandbox. GitHub MCP
(`gh` CLI itself is unauthenticated, but the MCP server works) was the only reachable data source. `actions_run_trigger`
(workflow_dispatch on `heartbeat-monitor.yml`) returned `403 Resource not accessible by integration` — could not force
a fresh check.

**What this means for confidence**: activity/liveness/deployment-freshness below are solid (derived from real GitHub
Actions logs + commit history). **Balance, open positions, and recent trade detail could NOT be verified this
session** — flagged explicitly per environment rather than guessed.

---

## Environment: PRODUCTION (Railway `main`)

### Activity
- Strategy/symbol: `hyper_growth` / ETHUSDT, `--max-position 0.5` (from `railway.json` `startCommand`).
- Mode: **LIVE** (real capital) — confirmed via heartbeat-monitor output: `active session 20 (LIVE)
  'HyperGrowth_ETHUSDT_20260605_205945'` (session started 2026-06-05 20:59:45 UTC — running ~28 days).
- Last confirmed heartbeat (`account_history` write): **0.9h old as of 2026-07-03 05:30:45 UTC** (i.e. last write
  ≈04:36 UTC). That data point is itself **~2h45m stale** at report time (08:15 UTC) because the `heartbeat-monitor`
  GitHub Actions cron (every 30 min, 120 min staleness threshold) has not run since 05:30 — a scheduling gap, not a
  bot-side signal. Could not force a fresh run (403 on `workflow_dispatch`).
  - 30/30 heartbeat-monitor runs over the last ~48h (2026-07-01 03:48 → 2026-07-03 05:30) were **success** — no
    stale-heartbeat alert in that window.
- Deployment freshness: `main` HEAD = `102c5d7` ("Promote to production: develop @ 61597b4 + review fixes (#830)"),
  merged **2026-06-16 16:47 UTC** — CI green, two independent code reviews approved per commit message. No commits to
  `main` since (17 days stale relative to `develop`, which is expected cadence, not itself an anomaly).

### Balance
- **Not verifiable this session** — no DB access, no health endpoint reachable (no local running process to hit).

### Open positions
- **Not verifiable this session.**

### Recent trades (24-48h)
- **Not verifiable this session.**

### Health / errors
- No Railway log access → cannot scan for the LESSONS.md §5.2 critical markers (`emergency.close`, `CLOSE-ONLY MODE`,
  `code=-1111`/`51077`, `Margin position check failed`, etc.) directly this session.
- **Elevated concern (not a fresh incident, but newly confirmed live-relevance)**: open GitHub issue **#734** (P0,
  `state:proposed`, unresolved) — "partial exits/scale-ins are bookkeeping-only in the live engine (no exchange order)
  AND mix fraction-of-original with fraction-of-balance units — desyncs tracked size from real holdings, books
  phantom PnL." Confirmed this session: `src/strategies/hyper_growth.py` sets `scale_in_thresholds: [0.015, 0.03]`,
  `scale_in_sizes: [0.40, 0.25]`, `max_scale_ins: 2` — **scale-ins are enabled** in the exact strategy running live
  right now, not a dormant code path. Added a comment to #734 documenting this link. Recommend a
  tracked-vs-exchange position-size reconciliation check as the fastest way to confirm/rule out drift.
- Other open P0/P1 `area:live-ops` issues (all `state:proposed`, none newly triggered this session, listed for
  context): #669 (no reconcile/error observability), #683 (clean-restart adopt-all may resurrect phantom positions),
  #689, #697 (SHORT guard blocked by ETH dust — known, has `needs:human-input`), #735–#739 (balance-ledger races,
  market-close quantity drift, split-transaction crash recovery, dead rate-limit retry decorators).

### Deployment status
- No restart/crash count available (no Railway access). Last known-good promotion 2026-06-16; 30/30 recent heartbeat
  checks over ~48h succeeded, which is inconsistent with a crash-looping process but does **not** rule out a
  degraded-but-alive state (per LESSONS.md: deploy API / even the heartbeat's own liveness proxy can be green while
  specific subsystems misbehave).

---

## Environment: STAGING (Railway `staging`)

### Activity
- Strategy/symbol: `hyper_growth` / ETHUSDT (same strategy family as production; staging config not independently
  confirmed beyond the heartbeat session name).
- Mode: **PAPER** — confirmed via heartbeat-monitor output: `active session 14 (PAPER)
  'HyperGrowth_ETHUSDT_20260617_081712'` (session started 2026-06-17 08:17:12 UTC — running ~16 days, consistent with
  the staging sync commit `883ac64` at 2026-06-17 08:13 UTC).
- Last confirmed heartbeat: **0.0h old (fresh) as of 2026-07-03 05:30:45 UTC**. Same monitoring-cadence caveat as
  production applies — no confirmed data point since 05:30 UTC.
- Deployment freshness: `staging` HEAD = `883ac64` ("Sync staging to develop (pick up prod review fixes) (#832)"),
  2026-06-17 08:13 UTC.

### Balance
- **Not verifiable this session** (paper-trading balance also lives in the same inaccessible DB).

### Open positions
- **Not verifiable this session.**

### Recent trades (24-48h)
- **Not verifiable this session.**

### Health / errors
- No Railway log access. Since staging runs the same `hyper_growth` code with scale-ins enabled, the #734 concern
  above applies equally here (paper capital only, so no real-money risk, but paper P&L numbers would be equally
  unreliable if the bug is live).

### Deployment status
- No restart/crash count available. Same 30/30 clean heartbeat-check history applies (checks don't distinguish
  environments in the run list — both are checked in the same job/step, see log excerpt below).

---

## Evidence: heartbeat-monitor job log (run 28640581232, 2026-07-03T05:30:43Z)

```
[staging] OK — last heartbeat 0.0h ago, active session 14 (PAPER) 'HyperGrowth_ETHUSDT_20260617_081712'
[production] OK — last heartbeat 0.9h ago, active session 20 (LIVE) 'HyperGrowth_ETHUSDT_20260605_205945'

All heartbeats healthy (threshold 120m).
```

30 consecutive `heartbeat-monitor.yml` runs from 2026-07-01T03:48:48Z through 2026-07-03T05:30:32Z were all
`conclusion: success` (no stale-heartbeat failures in this window). Cadence is irregular (scheduled every 30 min but
GitHub delayed some runs by 1-4h — a known behavior for low-traffic-repo cron jobs, not itself a bot-health signal).

## Other findings (context, not new)

- 7 open `type:incident` GitHub issues, all "[Automated] Weekly model training failed" (#605, #607, #614, #615, #618,
  #622, #707), all `priority:p2`, `state:proposed`, root cause "Verify required secrets are present" (AWS/SageMaker
  secrets). Recurring weekly since at least 2026-04-19, unresolved. Not live-bot-affecting (training pipeline only),
  but a standing gap worth closing.
- One `Optimizer MVP` GitHub Actions run failed today (2026-07-03T06:44:47Z, run 28643504271, on `develop`). This is
  a dev-side strategy-parameter sweep, unrelated to the production/staging trading processes — not investigated
  further as out of scope for this environment-health check.
- Charter (`​.claude/state/charter.md`) is still all-TODO (operating mode, capital, escalation method unfilled). This
  was already known from the 2026-06-04 snapshot; re-confirmed still open.

## Anomalies summary

| # | Severity | Description |
|---|---|---|
| 1 | P1 (existing, newly confirmed live-relevant) | #734 — live-engine scale-in bookkeeping bug is confirmed active in the exact strategy (`hyper_growth`) trading real capital in production right now. Recommend a tracked-vs-exchange position reconciliation check. |
| 2 | P2 | Cannot verify balance / open positions / recent trades for either environment — no DB/Railway access in this session. |
| 3 | P2 | `heartbeat-monitor.yml` (the only working liveness signal available here) hasn't run in ~2h45m at report time; last confirmed data point is from 05:30 UTC. Not itself evidence of an outage (30/30 prior success), but reduces confidence of "as of now" claims. |
| 4 | P3 | 7 recurring weekly-training-failure incidents (AWS/SageMaker secrets), unresolved since April. |
| 5 | P3 | Charter still all-TODO; escalation method/SLA undefined — if a real P0 were found, there is no confirmed paging path. |

## Actions taken

- Read charter, LESSONS.md §5, incidents dir (empty besides README), log.md track-record grep (no prior entries).
- Confirmed no Railway CLI / DB / Docker access in this sandbox (same as 2026-06-04).
- Used GitHub MCP (`get_me` succeeded despite `gh` CLI being unauthenticated) to inspect commit history, open
  issues/incidents, and Actions run history.
- Found and read `.github/workflows/heartbeat-monitor.yml` + `scripts/check_heartbeat.py` (read-only DB liveness
  check with real Railway DB credentials via Actions secrets) — used its **last successful run's logs** as the best
  available liveness/session evidence for both environments.
- Attempted `workflow_dispatch` on `heartbeat-monitor.yml` for a fresh check — denied (403, insufficient integration
  permissions). Did not retry / did not attempt any other mutating action.
- Cross-referenced `railway.json` (`startCommand`) and `src/strategies/hyper_growth.py` to confirm production's live
  strategy config includes enabled scale-ins, connecting it to open issue #734.
- Added a comment to GitHub issue #734 documenting the live-relevance finding (read/report action, no code or config
  changed).

## Escalations

- No new incident file created — #734 already exists and covers the underlying bug; a comment was added rather than
  a duplicate. Reporting directly to the requesting human now (this snapshot) constitutes the escalation for the
  newly confirmed live-relevance; no separate paging channel exists (charter escalation method is unfilled).
- Recommend: human/PM re-triages #734 priority given confirmed live exposure, and someone with DB/Railway access
  runs an exchange-vs-tracked-position reconciliation on the production ETHUSDT position to rule out drift.
