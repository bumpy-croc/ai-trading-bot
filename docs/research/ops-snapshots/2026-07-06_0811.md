# Ops Snapshot — 2026-07-06 08:11 UTC

**Overall**: yellow (cannot independently verify green; one known unresolved P1 stands; direct observability tooling unavailable this session)
**Environment**: both (production = live, staging = paper per charter)

---

## CRITICAL CAVEAT — no direct Railway/DB access this session

This sandbox has **no `RAILWAY_API_TOKEN`/Railway CLI auth** (`railway whoami` → `Unauthorized`, no MCP Railway connector installed) and **no `DATABASE_URL`** (not set; no local Postgres; Docker daemon unreachable). Every item below marked "inferred from git/GitHub" is **not a live read** — it is the best available proxy (deployed code state, config files, GitHub issue history, prior `.claude/state/log.md` entries). Nothing here should be treated as confirming or denying the bot's actual current balance, positions, or process health. **This is itself a P2 finding**: a live-ops session cannot do its job without these credentials wired into the environment — flagged below, not filed as a trading incident (it's a tooling/environment gap, not a bot defect).

GitHub access *did* work via the `mcp__github__*` tools (the `gh` CLI itself is unauthenticated/blocked, but the MCP server is not) — issue/PR history below is a live read as of this session.

---

## Environment: PRODUCTION (live, ETHUSDT, HyperGrowth strategy)

### 1. Deployment status
- Cannot query Railway directly. **Inferred from git**: `origin/main` HEAD is `e18ea25` (merge PR #905, "Promote to production: develop parity", 2026-07-05 19:37 BST / 18:37 UTC) — the most recent commit on `main`.
- `railway.json` `startCommand`: `atb live-health hyper_growth --max-position 0.20` — confirms strategy=HyperGrowth, cap wired at 0.20 (matches charter's 10% typical / 20% flagged-large threshold), matching the #836 fix.
- Cannot confirm the deployed container actually reflects `e18ea25` (Railway deploy could be stuck, failed, or on an older commit) — **this requires a Railway MCP/CLI session to verify**.

### 2. Current activity
- Strategy/symbol per charter + `railway.json`: **HyperGrowth / ETHUSDT**, live mode, capital ~$87 (per charter, last human-updated 2026-07-03).
- Open positions: **unknown** — no DB access. Last known state (per incident file, 2026-07-04 13:00 UTC): one open SHORT position, stop-protected.
- Cannot confirm the bot is currently running or trading.

### 3. Balance / PnL
- **Unknown this session** — no DB access. Last documented true-equity read (per `.claude/LESSONS.md` §5.6 and log.md correction, 2026-07-04): **$83.92** (2026-07-04 10:40 UTC), drawdown baseline reset at 2026-06-05 (~$84.40 peak) → ~0.6% DD at that time. That is a **2-day-stale** number, not current.
- No fresher balance/PnL figure available without DB access.

### 4. Errors / anomalies (last ~24h)
- No log access this session (no Railway logs). Cannot confirm or rule out anything in LESSONS.md §5.2 (emergency-close, circuit-breaker trip, SL failures, etc.).
- **Code-state check** against the 4 control failures named in incident/issue #845:
  1. Live max-drawdown hard-cap halt (`check_drawdown` had zero callers) — **FIXED on main** (`9eccadd` / PR #849, "enforce max-drawdown hard cap with close-only halt"; new `MaxDrawdownGuard`/`MaxDrawdownEnforcer`).
  2. Live drawdown-guard peak sourced from `account_history` instead of resetting on restart — **FIXED on main** (`fe6b2f1` / PR #851).
  3. HyperGrowth's loosened `dynamic_risk` breakers (`[0.15,0.30,0.45]`/`[0.8,0.5,0.2]`, second tier past the 20% kill line) — **NOT fixed**: verified directly in `origin/main:src/strategies/hyper_growth.py` lines 312-318, values unchanged.
  4. `backtest --max-drawdown` CLI default 0.5 vs `DEFAULT_MAX_DRAWDOWN=0.20` in constants — **NOT fixed**: verified `origin/main:cli/commands/backtest.py` still defaults to `0.5`.
  5. `alert_webhook_url` wiring — code shipped (`20788b7` / PR #864, "deliver operator alerts via $ALERT_WEBHOOK_URL"), but whether the env var is actually **set** in Railway prod is a config fact this session cannot check.
- Separately, issue **#734** (P0, `state:proposed`, still open) — live partial-exit/scale-in bookkeeping bug (phantom PnL, desync from real holdings) — interim mitigation **is** in code on `main`: `enable_partial_operations` defaults to `False` behind the `live_partial_operations` feature flag (`trading_engine.py:516-543`), so the underlying bug should not currently be exploitable in prod unless that flag is explicitly turned on.
- Issue **#669** (P0, `state:proposed`, still open) — `system_events` only logs START/STOP, `reconciliation_audit_events` is empty — i.e. **prod has no working error/anomaly telemetry of its own**. This compounds the "no DB access this session" problem: even with DB access, historical silent failures (a prior 12.8-day outage went undetected) would not necessarily show up as errors.

### 5. Open incidents
- **`.claude/state/incidents/2026-07-04T1300-P1-hypergrowth-drawdown-cap-breach.md`** — `status: open`, frontmatter severity `P1` (not yet reclassified in frontmatter despite a same-day correction recommending P1→P2). Matching **GitHub #845** — `open`, `state:proposed`, `priority:p1`. Summary: the headline "20% cap already breached, 20.33% DD" claim was **withdrawn** (phantom pre-2026-06-03 book-value peak, see LESSONS §5.6); true baseline DD was ~0.6% as of 2026-07-04. What still stands: the HyperGrowth strategy's own 365-day backtest structurally breaches the 20% cap (-20.15%/21.84% MaxDD) under its current config, and 2 of 4 control-layer fixes (items 3 and 4 above) remain unshipped. **Do not duplicate this incident** — it is already tracked; the new information from this session (fixes #848/#850/#855 shipped, #835/#836/#734 mitigations shipped) is worth appending to it or to `log.md`, not re-filing.
- No other open `type:incident` issues touch production/ETHUSDT.

---

## Environment: STAGING (paper)

### 1. Deployment status
- Cannot query Railway directly (same access gap).
- **Inferred from git**: `origin/staging` HEAD is `b47254b` (merge PR #904, 2026-07-05 19:24 BST), which is **13 commits behind `origin/main`** — staging is missing the same 13 commits main has, including the drawdown-hard-cap fix (#848), peak-seeding fix (#850), alert-webhook wiring (#855), and the original max-position/#835 sizing fix chain up through `102c5d7` (promote-to-production commit) — wait, checking more carefully: staging's last sync point predates main's latest promotion. In short, **staging is running an older, less-hardened build than production** right now (unusual — normally staging leads main). Worth confirming this is intentional (e.g., main was hot-fixed directly and staging sync is pending) rather than a missed promotion step.
- `dashboard.railway.json` (separate service) `startCommand`: `atb dashboards run monitoring --host 0.0.0.0` — this is the **monitoring dashboard** service config, not the trading bot itself; the trading-bot staging service presumably uses the same `railway.json` `startCommand` as prod (paper-trading variant) but this could not be confirmed without Railway project inspection.

### 2. Current activity / 3. Balance / 4. Errors
- **Unknown** — no DB or log access this session for staging either.

### 5. Open incidents
- No open `type:incident` GitHub issues reference staging specifically. Issue #669 (no error/reconcile observability) was originally discovered via a "staging anomaly sweep 2026-06-04" and is still open — implying staging's telemetry gaps are also unresolved.

---

## Anomalies summary

| # | Severity | Description |
|---|---|---|
| 1 | P2 | **This session has no Railway CLI auth and no DATABASE_URL** — cannot verify live process health, current balance, open positions, or recent logs for either environment. Tooling/environment gap, not a bot defect; needs credentials wired into future live-ops sessions. |
| 2 | P1 (existing, tracked) | Incident `2026-07-04T1300-P1-hypergrowth-drawdown-cap-breach` / GH #845 remains open. Live-breach claim was withdrawn, but HyperGrowth's own 365d backtest still structurally breaches the 20% max-drawdown cap, and 2 of its 4 named control-gaps (dynamic_risk loosening, backtest CLI default) are still unfixed on `main` as of this session. |
| 3 | P1 (existing, tracked) | GH #669 (no error/reconcile observability — `system_events`/`reconciliation_audit_events` effectively empty) still open. Prod/staging currently have no self-reported anomaly telemetry; this session's inability to see logs compounds it. |
| 4 | P2 (existing, tracked) | GH #734 (live partial-exit/scale-in phantom-PnL bug) still open, but interim mitigation (feature flag default-off) is confirmed present in `main`'s code — should not be live-exploitable unless the flag was explicitly enabled in Railway env vars (unverifiable this session). |
| 5 | P2 (new observation) | `origin/staging` is 13 commits behind `origin/main`, missing the drawdown-hard-cap, peak-seeding, and alert-webhook fixes that are already on production. Staging trailing prod (rather than leading it) is inverted from the normal promotion flow — worth a sanity check that this isn't masking a stalled staging sync. |

## Actions taken
- Read charter, incidents, `log.md` tail, LESSONS.md §5 signatures (no changes made).
- Attempted `railway whoami` (unauthorized), checked env for `DATABASE_URL`/`RAILWAY_API_TOKEN` (absent), checked Docker/Postgres locally (unavailable) — confirmed no direct observability path exists in this sandbox.
- Confirmed GitHub MCP tools work despite `gh` CLI being blocked; used them to check open `type:incident`/`priority:p0` issues and cross-reference against code state on `origin/main`.
- Diffed `origin/main` vs `origin/staging` vs `origin/develop` to establish deployed-code proxies for both environments.
- Verified in the actual source at `origin/main` which of incident #845's 4 named control-layer fixes have shipped (2 shipped, 2 outstanding) and that #734's interim mitigation (feature-flag default-off) is present.
- No restarts, deploys, or config changes performed (monitor-only).

## Escalations
- **None raised as new** — the two live P1 issues (#845, #669) are already open and owned; not duplicating. Recommend to pm/human: (a) provision this live-ops session type with a read-only `RAILWAY_API_TOKEN` and a read-only `DATABASE_URL` so future snapshots can actually verify live state instead of relying on code-state inference; (b) confirm whether the staging trailing prod by 13 commits is expected.
