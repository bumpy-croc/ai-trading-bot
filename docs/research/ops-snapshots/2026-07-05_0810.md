# Ops Snapshot — 2026-07-05 08:10 UTC

**Overall**: yellow (unverifiable, not confirmed-healthy)
**Environment**: staging (paper trading, Railway env `staging`, branch `staging`)

## Tooling gap (blocks direct verification)

This session has **no live telemetry access**: no `railway` binary, no `DATABASE_URL`/DB
credentials, no `docker` daemon, no `atb` installed (`.venv` exists but is empty/not
provisioned). `git remote` points at a proxy with no `staging` branch fetched locally.
Everything below items 1-3 (process, DB, live logs) is therefore **not directly checked this
session** — reported from state files / GitHub only. This gap itself is the top finding: a
live-ops snapshot that cannot reach Railway or Postgres should not be read as "confirmed green."

## Process / Railway (staging)
- Not verifiable this session (`railway` CLI absent). Last known-good pattern per LESSONS.md:
  target with `railway logs -e staging -n 400` from a linked repo checkout; `-e staging`, not
  `production`.

## Database (staging)
- Not queried this session (no `DATABASE_URL`). Runbook (`docs/operations_runbook.md`) expects
  `psql "$DATABASE_URL"` for `positions`, `trades`, `account_balances`, `account_history`.
- Known historical fact worth carrying forward: staging suffered a **12.8-day fully silent
  outage** (2026-05-19 → 2026-06-01, 306h `account_history` gap, zero `strategy_executions`)
  while `system_events` showed nothing but START/STOP — see GH #669 (P0, still **open**).
  A write-gap dead-man's-switch to catch a repeat is proposed in #689 (P1, still **open**, not
  yet shipped). **Practical implication: a quiet staging DB is not proof of health** until #689
  ships — a fresh gap of this kind would currently go undetected by automation.

## Recent trading activity / open positions
- Not available (no DB access this session).

## Logs / error signatures
- Not pulled this session. Per LESSONS.md §5.4, staging is expected to run clean of the
  margin-user-stream churn that affects prod (`TRADING_MODE=paper` never opens a margin
  user-data socket) — so if logs are pulled later, that specific noise class should NOT appear
  on staging; its presence would itself be an anomaly (wrong TRADING_MODE / config drift).

## Open incidents concerning staging
- **#669** (P0, open, `type:post-mortem-action`): staging/prod observability gap — `system_events`
  only emits START/STOP, `reconciliation_audit_events` never populated. Root cause of the silent
  12.8-day staging outage above. Part 1 shipped (PR #688); part 2 (**#689**, P1, open) — write-gap
  dead-man's-switch + reconciler audit emission — not yet shipped.
- **#616** (P2, open): staging is *clean* of the margin-ws churn described (runs paper, no margin
  socket) — but flags that **staging and production share an identical live-capable
  `BINANCE_API_KEY`/`SECRET`**. A paper environment holding live-capable keys is a standing
  security concern, not an active incident.
- **#845 / P1 incident `2026-07-04T1300-...-hypergrowth-drawdown-cap-breach`**: concerns
  **production** capital and the HyperGrowth backtest's structural 21.84% MaxDD, corrected same-day
  to withdraw the "already breached live" claim. Relevant to staging only insofar as staging may
  run the same strategy config under paper validation — worth confirming staging's HyperGrowth
  instance (if any) inherits the same loosened `dynamic_risk` breaker override
  (`hyper_growth.py:294-300`) before trusting its paper drawdown numbers as representative.

## Active proposals / experiments on staging
- **#887** (`ETHUSDT/basic/2026-07-04_22h_v1`, native price model): trained + evaluated
  (holdout RMSE 0.065, directional accuracy 0.53), backtested favorably vs. the cross-symbol
  baseline it replaces (-1.31% vs -3.29% over 90d). Lifecycle checklist: Trained ✓, Evaluated ✓,
  Proposed via PR #886 ✓, **Paper: not started** — blocked on #886 merging to develop and staging
  picking it up. **This is the model to watch**: once #886 merges, the 48h paper-trading clock
  (mandatory per `ml-engineer` promotion bar / CLAUDE.md) starts on staging. No live-promotion
  proposal exists yet — correctly gated on that clock plus risk-officer review.
- **#844** (`kelly_momentum` Kelly-active evaluation): recommends `kelly_momentum` "earns a staging
  paper trial alongside (never replacing) live HyperGrowth" — recommendation stands, but no
  confirmation found this session that the trial has actually started on staging (no PR/issue
  found wiring it in yet). Worth a follow-up check once #886/#887's model paper trial is also
  running, so the two don't get conflated in staging logs.

## Anomalies
- **[P2] Tooling/access gap**: this session cannot reach Railway or the staging Postgres DB
  directly — no `DATABASE_URL`, no `railway` CLI, no `atb`. Recommend the human confirm whether
  future live-ops sessions are expected to run with these provisioned (env vars / CLI installed
  via the sessionStart hook) — right now a snapshot request produces state-file/GitHub-only
  coverage, not the DB/process ground-truth the runbook calls for.
- **[P1, open, pre-existing]** #669/#689: staging observability gap — a repeat of the 12.8-day
  silent outage would currently go undetected. Not new; flagging because it directly limits what
  "staging looks fine" can mean until #689 ships.
- No new P0 found. No duplicate incident filed.

## Actions taken
- Read-only: charter, incidents dir, log.md tail, LESSONS.md §5, GitHub issues/incident (`gh`
  CLI unauthenticated in this sandbox — used `mcp__github` tools instead, which worked).
- No restarts, no config changes, no DB writes.

## Escalations
- None raised as a new incident. The tooling-access gap and the still-open #669/#689 pair are
  surfaced here for pm/human awareness, not filed as a fresh incident (both are already tracked
  or are a session-environment issue rather than a bot-state issue).
