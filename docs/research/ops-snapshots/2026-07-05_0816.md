# Ops Snapshot — 2026-07-05 08:16 UTC

**Overall**: yellow (unverifiable this session, not confirmed-healthy)
**Environment**: production (live, Railway env `production`, service "Trading Bot", branch `main`)

## Tooling gap (blocks direct verification) — top finding

This session (Claude Code Web / remote, `CLAUDE_CODE_REMOTE=true`) has **no live production
telemetry access**:
- `railway` binary: not installed (`which railway` → not found).
- `DATABASE_URL`: unset — no `psql`/`atb db verify` route to the production Postgres.
- `atb` CLI: not installed (`.venv` not provisioned with the trading-bot package in this session).
- `gh` CLI: `GITHUB_TOKEN` present but **invalid** ("Failed to log in... token is invalid");
  worked around via the `mcp__github__*` tools instead, which authenticate successfully.

Consequence: items 1-3, 5-8 of the standard health snapshot (process status, health endpoint,
recent trades, open positions, data freshness, error-log scan, Railway deploy/restart history)
are **not independently verified this pass** — reported below from `.claude/state/log.md` and the
GitHub issue tracker only, which is stale by construction (last live-state update: 2026-07-04
13:55 UTC, ~18.5h old at snapshot time). A prior session hit the identical gap against **staging**
(`docs/research/ops-snapshots/2026-07-05_0810.md`), confirming this is an environment-provisioning
issue, not a one-off. **Recommend**: either provision this session type with a read-only
`DATABASE_URL` + `railway` CLI, or route live-ops passes to a session/host that has them — right
now a "snapshot" from this context is state-file/GitHub coverage only, not ground truth.

## Process / Railway (production)
- Not verifiable this session. Known config (`railway.json`): `startCommand: atb live-health
  hyper_growth --max-position 0.20`, `healthcheckPath: /health`, `restartPolicyType: ON_FAILURE`
  (max 10 retries). No public health-endpoint URL recorded in repo docs to `curl` externally.
- Per LESSONS.md §5.1, deploy-API "SUCCESS" is not sufficient for liveness even when reachable —
  ground truth requires a recent `Decision:`/`Status:` log line plus the hourly `account_history`
  heartbeat, neither of which could be pulled this pass.

## Database (production)
- Not queried this session (no `DATABASE_URL`). Last known figures come from log.md, not a fresh
  read:
  - 2026-07-04 10:40 UTC: equity **$83.92**.
  - Corrected drawdown baseline (pm, 2026-07-04): peak = peak **true** equity since the last
    reconciled reset (2026-06-05 / session 20, ≈$84.40) → true DD **≈0.6%** at that time (the
    earlier same-day claim of a 19-20% live breach was withdrawn same-day as a phantom
    pre-#655 book-value artifact — see incident file CORRECTION section).
  - Standup tripwires in force: **$80.18 / $75.96 / $67.52**.
- No fresher balance/position/trade read obtained this pass — cannot confirm current equity,
  open positions, or last-24h trade activity independently.

## Recent trading activity / open positions
- Not available (no DB access this session). Last noted open exposure: one SHORT ETHUSDT
  position as of 2026-07-04 (per incident file, now superseded/unknown — do not treat as current).

## Logs / error signatures
- Not pulled this session (no `railway logs` access). No fresh scan against LESSONS.md §5.2
  critical markers (`emergency.close`, `CLOSE-ONLY MODE`, `ACCOUNT CIRCUIT BREAKER TRIPPED`,
  `code=-1111`/`code=51077`, `-2010`, `Margin position check failed`, unexpected restart) was
  possible.

## Open incidents concerning the live bot
- **GH #845 / `.claude/state/incidents/2026-07-04T1300-P1-hypergrowth-drawdown-cap-breach.md`**
  — status **open** (frontmatter still says P1; pm recommended reclassifying to P2 in a same-day
  correction, not yet applied to the file's frontmatter). Structural finding stands regardless of
  the withdrawn live-breach claim: HyperGrowth's exact 365d backtest reproduces -20.15%
  return/21.84% MaxDD on the current live config, four control-layer failures are confirmed
  (#749 dead-code drawdown halt, backtest `--max-drawdown` default 0.5 vs 0.20, HyperGrowth's
  loosened `dynamic_risk` breaker override past the 20% line, live peak-amnesia on restart), and
  `alert_webhook_url` is unset in prod (nothing pages even if a control does fire). Containment
  proposal `2026-07-04-02-hypergrowth-drawdown-containment.md` is filed; step 1 (entry-pause)
  downgraded to "tripwires binding, no immediate pause" per the correction.
- No other open `type:incident` issues found (`mcp__github__list_issues` filtered to
  `type:incident`, state OPEN → only #845 returned).
- No P0 found or filed this pass.

## Anomalies
- **[P2] Tooling/access gap (this session)**: cannot reach Railway or the production Postgres
  directly. Second consecutive session to hit this (staging pass, same morning). Recommend the
  human/PM decide whether to provision credentials for this session type or restrict live-ops
  passes to a provisioned host.
- **[P2, pre-existing, carried forward]** #845: structural 21.84% backtest MaxDD on current live
  HyperGrowth config, still not contained (proposal steps 2-4 not yet landed as of last log entry).
- **[Data-freshness flag]** All production figures in this snapshot are ≥18.5h old at time of
  writing — do not treat as current; re-run from a provisioned session before relying on the
  balance/position numbers above for any decision.
- No new anomaly independently detected this pass (nothing *could* be independently detected).

## Actions taken
- Read-only: `.claude/state/charter.md`, `.claude/state/risk-limits.json`, `.claude/LESSONS.md`
  §5, `.claude/state/incidents/*.md`, `.claude/state/log.md` tail, `mcp__github__list_issues`
  (label `type:incident`, state OPEN), env/tooling probes (`which railway`, `atb --version`,
  `gh auth status`, `DATABASE_URL` check).
- No restarts, deploys, config edits, or DB writes attempted or made.

## Escalations
- None raised as a new incident this pass — #845 already covers the only substantive open
  production concern and is already filed/open. The tooling-access gap is surfaced here for
  pm/human awareness (recommend a provisioning decision), not filed as a fresh incident.
