name: Deploy to Staging

on:
  workflow_dispatch:
    inputs:
      service_id:
        description: 'Override service ID (optional)'
        required: false
        type: string
      commit_sha:
        description: 'Specific commit SHA to deploy (defaults to latest)'
        required: false
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.commit_sha || github.sha }}

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway (staging)
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
          SERVICE_ID: ${{ inputs.service_id }}
        run: |
          if [ -n "$SERVICE_ID" ]; then
            railway up --project $RAILWAY_PROJECT_ID --service $SERVICE_ID --environment staging --detach
          else
            railway up --project $RAILWAY_PROJECT_ID --environment staging --detach
          fi

      - name: Post deployment info
        run: |
          echo "Deployment triggered for staging environment."
          echo "Commit SHA: ${{ inputs.commit_sha || github.sha }}"
          echo "Service ID: ${{ inputs.service_id || 'default' }}" 