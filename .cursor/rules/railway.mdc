- App is deployed on Railway; see `railway.json` and `dashboard.railway.json`.
- Use project/service names from Railway UI for your workspace; URLs vary per env.
- Database access is managed via Railway; prefer app config over hardcoded URLs.
- For CLI, see `https://docs.railway.com/reference/cli-api`.